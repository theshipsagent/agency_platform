// TODO Phase B: documents tab
export default function PortCalldocumentsPage({ params }: { params: { id: string } }) {
  return <div className="p-8"><h1 className="text-2xl font-bold">documents</h1><p className="text-muted-foreground mt-2">Port call {params.id} — documents coming in Phase B</p></div>
}
