// TODO Phase B: timeline tab
export default function PortCalltimelinePage({ params }: { params: { id: string } }) {
  return <div className="p-8"><h1 className="text-2xl font-bold">timeline</h1><p className="text-muted-foreground mt-2">Port call {params.id} — timeline coming in Phase B</p></div>
}
